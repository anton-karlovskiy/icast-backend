# Environment Configuration
NODE_ENV=development
PORT=4000

# JWT Configuration
JWT_SECRET=your_jwt_secret_here

# MongoDB Configuration
DEV_MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database-name?retryWrites=true&w=majority
PROD_MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database-name?retryWrites=true&w=majority

# External API Configuration
GOAL_SERVE_API_KEY=your_goalserve_api_key_here

# Cookie Environment (for development)
COOKIE_ENV=development

# Optional: Additional MongoDB Configuration
# MONGODB_URI=mongodb://localhost:27017/icast-backend

# Optional: Email Configuration (if using email features)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your_email@gmail.com
# EMAIL_PASS=your_email_password

# Optional: Phone Verification (if using SMS features)
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_PHONE_NUMBER=your_twilio_phone_number
